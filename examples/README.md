# Examples

`sample_outputs/` contains sample JSON outputs from earlier Windows lab runs.

These are here so Copilot/coding agents can understand the current report shapes.

They do not include GTA keys or RPF archives.
